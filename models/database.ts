/** The web database.ts file will not store or retrieve any values
 * since the Expo SQLite package doesn't work on web. Instead,
 * values are hardcoded in for testing and debugging purposes.
 *
 * Any database related testing needs to take place on an Android,
 * iOS, or emulator device.
 */
export async function createDatabase() {
  console.log("creates database");
}
